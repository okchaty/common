---
# defaults file for common

# common_deployment_apps update code deployment

common_packages: []
common_user: ubuntu

# common_create_files:
#   - path: "{{ app_dir_home }}/.ssh"
#     state: "directory"
#     owner: "{{ user }}"
#   - path: "{{ apps_path }}/src/assets"
#     state: "directory"
#     owner: "{{ user }}"

# common_deployment:
#     - name: "{{ app_name }}"
#     version: "{{ git.branch.deployment }}" # Could be a hash, branch or tag name
#     repo: "git@bitbucket.com:labpositiva/fbp"
#     force: yes
#     location: "{{ apps_path }}"
